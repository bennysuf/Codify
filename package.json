{
    "name": "codify-deploy",
    "engines": {
      "node": "16.16.0"
    },
    "scripts": {
      "clean": "rm -rf public",
      "build": "npm install --prefix client && npm run build --prefix client",
      "deploy": "cp -a client/build/. public/",
      "postbuild": "npm run clean && npm run build && npm run deploy"
    }
  }